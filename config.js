const K="connect4_theme_v3",D={theme:"neon",sound:true,custom:{background:"#101020",panel:"#20203a",board:"#3454d1",button:"#7c3cff",text:"#ffffff",accent:"#31f7ff"}};
export const loadPreferences=()=>({...D,...(JSON.parse(localStorage.getItem(K)||"null")||{})});
export function savePreferences(v){localStorage.setItem(K,JSON.stringify(v));applyPreferences(v)}
export function applyPreferences(v){document.body.dataset.theme=v.theme;const r=document.documentElement;if(v.theme==="custom"){r.style.setProperty("--bg",v.custom.background);r.style.setProperty("--panel",v.custom.panel);r.style.setProperty("--panel2",v.custom.panel);r.style.setProperty("--board",v.custom.board);r.style.setProperty("--edge",shade(v.custom.board,-25));r.style.setProperty("--button",v.custom.button);r.style.setProperty("--accent2",v.custom.button);r.style.setProperty("--text",v.custom.text);r.style.setProperty("--accent",v.custom.accent);r.style.setProperty("--cell",shade(v.custom.background,-15))}else["--bg","--panel","--panel2","--board","--edge","--button","--accent2","--text","--accent","--cell"].forEach(p=>r.style.removeProperty(p))}
function shade(h,a){const x=h.replace("#",""),v=[0,2,4].map(i=>Math.max(0,Math.min(255,parseInt(x.slice(i,i+2),16)+a)));return`#${v.map(n=>n.toString(16).padStart(2,"0")).join("")}`}
